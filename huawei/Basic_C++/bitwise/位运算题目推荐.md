# 位运算题目推荐清单

## 一、CodeFun (P开头) 题目

### 入门级（⭐）

| 题号 | 题目名称 | 考察点 | 状态 |
|------|----------|--------|------|
| P191 | 位1的个数 | 统计二进制中1的个数 | ⬜ |
| P231 | 2的幂 | 判断是否为2的幂 | ⬜ |
| P342 | 4的幂 | 判断是否为4的幂 | ⬜ |
| P461 | 汉明距离 | 统计不同位的个数 | ⬜ |
| P476 | 数字的补数 | 按位取反 | ⬜ |
| P693 | 交替位二进制数 | 判断二进制位是否交替 | ⬜ |

### 进阶级（⭐⭐）

| 题号 | 题目名称 | 考察点 | 状态 |
|------|----------|--------|------|
| P136 | 只出现一次的数字 | 异或的性质 | ⬜ |
| P137 | 只出现一次的数字 II | 位运算统计 | ⬜ |
| P260 | 只出现一次的数字 III | 分组异或 | ⬜ |
| P201 | 数字范围按位与 | 找公共前缀 | ⬜ |
| P318 | 最大单词长度乘积 | 位掩码 | ⬜ |
| P371 | 两整数之和 | 位运算实现加法 | ⬜ |

### 高级（⭐⭐⭐）

| 题号 | 题目名称 | 考察点 | 状态 |
|------|----------|--------|------|
| P78 | 子集 | 位枚举子集 | ⬜ |
| P89 | 格雷编码 | 格雷码生成 | ⬜ |
| P187 | 重复的DNA序列 | 位哈希 | ⬜ |
| P421 | 数组中两个数的最大异或值 | 字典树+位运算 | ⬜ |

---

## 二、洛谷 (Luogu) 题目

| 题号 | 题目名称 | 考察点 | 难度 |
|------|----------|--------|------|
| P1100 | 高低位交换 | 位交换 | 入门 |
| P1225 | 黑白棋游戏 | 位运算模拟 | 普及- |
| P1469 | 找筷子 | 异或 | 入门 |
| P2114 | 起床困难综合症 | 位运算贪心 | 提高+/省选- |
| P3811 | 【模板】乘法逆元 | 快速幂 | 普及- |
| P5657 | [CSP-S2019] 格雷码 | 格雷码 | 入门 |

---

## 三、AcWing 题目

| 题号 | 题目名称 | 考察点 |
|------|----------|--------|
| 801 | 二进制中1的个数 | lowbit |
| 89 | a^b | 快速幂 |
| 90 | 64位整数乘法 | 位运算乘法 |

---

## 四、做题顺序建议

### 第一轮：基础操作
1. P191 - 位1的个数
2. P231 - 2的幂
3. P461 - 汉明距离
4. P476 - 数字的补数

### 第二轮：异或专题
1. P136 - 只出现一次的数字
2. P137 - 只出现一次的数字 II
3. P260 - 只出现一次的数字 III

### 第三轮：进阶应用
1. P78 - 子集
2. P89 - 格雷编码
3. P201 - 数字范围按位与
4. P318 - 最大单词长度乘积

---

## 五、关键技巧回顾

| 技巧 | 代码 | 用途 |
|------|------|------|
| lowbit | `x & -x` | 取最低位的1 |
| 消去1 | `x & (x - 1)` | 消除最低位的1 |
| 异或性质 | `a ^ a = 0` | 去重 |
| 判断2的幂 | `x && !(x & (x - 1))` | - |
| 统计1的个数 | Brian Kernighan算法 | - |

---

## 六、模板代码

### 1. 统计二进制中 1 的个数
```cpp
int count_ones(int x) {
    int cnt = 0;
    while (x) {
        x &= x - 1;
        cnt++;
    }
    return cnt;
}
```

### 2. 判断是否是 2 的幂
```cpp
bool isPowerOfTwo(int n) {
    return n > 0 && !(n & (n - 1));
}
```

### 3. 只出现一次的数字
```cpp
int singleNumber(vector<int>& nums) {
    int res = 0;
    for (int num : nums) res ^= num;
    return res;
}
```

### 4. 枚举所有子集
```cpp
vector<vector<int>> subsets(vector<int>& nums) {
    int n = nums.size();
    vector<vector<int>> ans;
    for (int mask = 0; mask < (1 << n); mask++) {
        vector<int> subset;
        for (int i = 0; i < n; i++) {
            if (mask & (1 << i)) {
                subset.push_back(nums[i]);
            }
        }
        ans.push_back(subset);
    }
    return ans;
}
```
